This is the help text. Obtain this document by using the key binding control shift "H" "M" on Windows, or command shift "H" "M" on Mac
To use Code Reader,
Open the C plus plus file you want to read
On Windows the command to run the plugin is control shift "C" "R"
On Mac the command to run the plugin is command shift "C" "R"
You can also toggle line numbers to be read to you
On Windows the command is control shift "L"
On Mac the command is command shift "L"
Use the arrow keys to cycle through the available functions or classes
Press enter to make a selection
End of the help document